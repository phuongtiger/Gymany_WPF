<Window x:Class="MyWPF.Views.Customer.AddCustomerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyWPF.Helper"
        mc:Ignorable="d"
        Title="AddCustomerView" Width="400" Height="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="74*"/>
        </Grid.ColumnDefinitions>

        <Border Width="300" Height="750" Background="White" CornerRadius="6"  Grid.Column="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Color="Gray" />
            </Border.Effect>

            <StackPanel Orientation="Vertical" Margin="20">
                <TextBlock Text="Add Customer" Foreground="#7B5CD6" FontSize="25" HorizontalAlignment="Center" FontWeight="Bold" Padding="10"/>

                <!-- Profile Image Section -->
                <Ellipse Width="150" Height="150" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,10,0,10"
                         Stroke="Black" StrokeThickness="1">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding ImageHelper.ImageSource}" Stretch="UniformToFill"/>
                    </Ellipse.Fill>
                </Ellipse>
                <Button Content="Upload Image" Background="#7B5CD6" Foreground="white" Width="100" Command="{Binding BrowseImageCommand}"/>

                <TextBlock Foreground="Red" Text="You are required to fill in the * fields" Margin="0,5,0,0"/>
                
                <!-- Username Field with Validation -->
                <TextBlock Text="Username*" Margin="0,10,0,5"/>
                <TextBox Margin="0,0,0,20">
                    <TextBox.Text>
                        <Binding Path="NewCustomer.CusUsername" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True">
                            <Binding.ValidationRules>
                                <local:MinLengthValidationRule MinLength="8"/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Validation.ErrorTemplate">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <StackPanel>
                                            <Border BorderBrush="Red" BorderThickness="1">
                                                <AdornedElementPlaceholder />
                                            </Border>
                                            <TextBlock Foreground="Red" Text="{Binding [0].ErrorContent}" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Password Field with Binding -->
                <TextBlock Text="Password*" Margin="0,0,0,5"/>
                <PasswordBox Margin="0,0,0,15" local:PasswordBoxBindingBehavior.BoundPassword="{Binding NewCustomer.CusPassword, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Name Field -->
                <TextBlock Text="Name" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NewCustomer.CusName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <!-- Birthday Field -->
                <TextBlock Text="Birthday" Margin="0,0,0,5"/>
                <DatePicker SelectedDate="{Binding NewCustomer.CusAge, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <!-- Address Field -->
                <TextBlock Text="Address" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NewCustomer.CusAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <!-- Email Field with Validation -->
                <TextBlock Text="Email*" Margin="0,10,0,5"/>
                <TextBox Margin="0,0,0,15">
                    <TextBox.Text>
                        <Binding Path="NewCustomer.CusEmail" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True">
                            <Binding.ValidationRules>
                                <local:EmailValidationRule />
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Validation.ErrorTemplate">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <StackPanel>
                                            <Border BorderBrush="Red" BorderThickness="1">
                                                <AdornedElementPlaceholder />
                                            </Border>
                                            <TextBlock Foreground="Red" Text="{Binding [0].ErrorContent}" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Phone Field with Validation -->
                <TextBlock Text="Phone*" Margin="0,10,0,5"/>
                <TextBox Margin="0,0,0,15">
                    <TextBox.Text>
                        <Binding Path="NewCustomer.CusPhone" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True">
                            <Binding.ValidationRules>
                                <local:PhoneValidationRule />
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Validation.ErrorTemplate">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <StackPanel>
                                            <Border BorderBrush="Red" BorderThickness="1">
                                                <AdornedElementPlaceholder />
                                            </Border>
                                            <TextBlock Foreground="Red" Text="{Binding [0].ErrorContent}" Margin="0,5,0,0"/>
                                        </StackPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                

                <!-- Add/Cancel Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="Add" Background="#7B5CD6" Foreground="white" Width="100" Command="{Binding AddCustomerCommand}" Margin="0,0,5,0"/>
                    <Button Content="Cancel" Background="#7B5CD6" Foreground="white" Width="100" Click="CancelButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
